{
  "buildCommand": "cd newsly-react && npm install && npm run build",
  "outputDirectory": "newsly-react/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
